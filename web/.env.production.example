# 프로덕션 배포 시 사용하는 환경 변수 예시
# Cloudflare Pages 배포 시에는 대시보드의 Environment Variables에 직접 입력하거나
# 이 파일을 .env.production 으로 복사 후 사용하세요.
# 주의: .env.production은 .gitignore에 포함되어 있으므로 git에 커밋되지 않습니다.

NEXT_PUBLIC_API_BASE=https://jnu-events-worker.YOUR_ACCOUNT.workers.dev
